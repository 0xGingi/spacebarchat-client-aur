[Unit]
Description=Spacebar, for better and secure communication

[Service]
User=
WorkingDirectory=/opt/spacebar-client
ExecStart=serve -s build
Restart=always
StandardError=journal
StandardOutput=journal

[Install]
WantedBy=multi-user.target
